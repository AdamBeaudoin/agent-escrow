# Local / VPS: copy .env.example → .env in judge-service, set MPP_* and secrets, then:
#   docker compose up --build

services:
  judge:
    build: ./judge-service
    ports:
      - "4102:4102"
    environment:
      PORT: "4102"
      HOST: "0.0.0.0"
    env_file:
      - ./judge-service/.env
